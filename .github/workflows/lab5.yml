name: My GitHub Actions Pipeline
on:
  push:
    branches: 
      - 'master'
  pull_request:
    branches:
      - 'master'   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code

    - name: Build code
      run: <BUILD>
